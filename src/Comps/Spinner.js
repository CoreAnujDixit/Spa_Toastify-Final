import React from 'react';
import './Spinner.css';

const Spinner = () => {
    return (
        <div className='Spinner'>

            <div class="loader"></div>
        </div>
    );
}

export default Spinner;
